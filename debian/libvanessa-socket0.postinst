#!/bin/sh

#DEBHELPER#

if [ "$1" = "configure" ]; then
    ldconfig
    if [ -d /usr/doc -a ! -e /usr/doc/libvanessa-socket0 -a -d /usr/share/doc/libvanessa-socket0 ]; then
      ln -sf ../share/doc/libvanessa-socket0 /usr/doc/libvanessa-socket0
    fi
fi
